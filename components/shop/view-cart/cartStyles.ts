/** Shared cart surface tokens — keep panel radius consistent with account pages (12px). */
export const CART_PANEL =
  "rounded-[12px] border border-[var(--line)] bg-[var(--white)]";

export const CART_PANEL_SOFT = "rounded-[12px] border border-[var(--line)] bg-[var(--bg)]";

export const CART_CONTROL = "rounded-[10px]";

/** Inset radius for toggles inside a `CART_CONTROL` shell with `p-[4px]`. */
export const CART_CONTROL_INNER = "rounded-[6px]";

/** Shared height for cart top controls (tenure pill + billing toggle). */
export const CART_CONTROL_HEIGHT =
  "h-[40px] min-h-[40px] max-h-[40px] box-border leading-none";
